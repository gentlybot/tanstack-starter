# Copy to .env for local development (TanStack Start/Vite, drizzle-kit, and
# the standalone worker/ws processes all load it).
# On gently, DATABASE_URL is injected automatically by the postgres service
# declared in gently/apps.yml, and the other values come from its env block.

# Postgres connection string (the only required variable).
DATABASE_URL=postgres://app:app@localhost:5432/app

# Signs better-auth session tokens. Any string works in dev; generate a real
# one for production: openssl rand -base64 32
BETTER_AUTH_SECRET=dev-only-insecure-secret-change-me

# Canonical public origin for auth links, redirects, and callbacks. Set this in
# production. Local request inference works, but an explicit value is steadier.
BETTER_AUTH_URL=http://localhost:3000

# Optional comma-separated host allowlist for deployments served from multiple
# domains or preview hosts. When set, Better Auth resolves the base URL from
# each request and rejects unknown hosts. Use hostnames, not full origins.
# BETTER_AUTH_ALLOWED_HOSTS=localhost:3000,www.example.com,*.preview.example.com

# Port for the standalone WebSocket server (npm run ws). If you change it,
# update the /ws proxy target in vite.config.ts too.
# WS_PORT=3001
